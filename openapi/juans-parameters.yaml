work:
  name: work
  in: query
  required: true
  description: 佛典編號
  example: T0001
  schema:
    type: string
juan:
  name: juan
  in: query
  required: true
  description: 卷號
  example: 1
  schema:
    type: integer
work_info:
  name: work_info
  in: query
  required: false
  description: >
    是否一併回傳「佛典資訊」

    * 預設為 0: 不回傳
    * 若設為 1: 回傳
  example: 1
  schema:
    type: integer
toc:
  name: toc
  in: query
  required: false
  description: >
    是否一併回傳目次

    * 預設為 0: 不回傳
    * 若設為 1: 回傳
  example: 1
  schema:
    type: integer