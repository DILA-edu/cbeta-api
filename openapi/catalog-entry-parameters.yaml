q:
  name: q
  in: query
  required: false
  description: Catalog Entry ID
  example: CBETA.001
  schema:
    type: string
vol:
  name: vol
  in: query
  required: false
  description: volume. This parameter is incompatible with `q`.
  example: T01
  schema:
    type: string