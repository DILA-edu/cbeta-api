facet:
  name: facet
  in: query
  required: false
  example: 1
  schema:
    type: integer
    enum: [0, 1]
    default: 0
  description: |
    0: 不回傳 facet。
    
    1: 回傳五種 facet： 藏經 (canon)、部類 (category)、作譯者 (creator)、朝代 (dynasty)、典籍 (work)。