%h1 搜尋 佛典標題（經名）

%p 例：#{link_to_url '/search/title?q=觀無量壽經'}

%p 模糊搜尋，至少符合3個字（查詢字串不足3個字時，則須全部符合）。

%p 結果依相關度排序。

=render partial: 'q-length-limit'

%h2 參數
%table.table
  %tr
    %td 參數名稱
    %td
    %td 說明
  =render partial: 'search-arg-paginate'
  %tr
    %td 限制搜尋範圍
    %td 選項
    %td
      請參考：
      =link_to nil, controller: 'static_pages', action: 'search_filter' 
