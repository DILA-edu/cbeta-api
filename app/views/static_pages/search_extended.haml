%h1 擴充模式 全文檢索

=render partial: 'q-quotes', locals: { path: '/search/extended' }

=render partial: 'q-length-limit'

%p 以下範例均要做 URL encode, 例如：

%pre "波羅蜜" | "波羅密"

%p 要編碼為

%pre %22波羅蜜%22%20%7C%20%22波羅密%22

%h2 AND
%p 多個字詞都必須出現。將每個詞用雙引號括起來，再以空格隔開多個字詞。
%p 例：#{link_to_url '/search/extended?q="法鼓" "聖嚴"'}

%h2 OR
%pre "波羅蜜" | "波羅密"
%p 例：#{link_to_url '/search/extended?q="波羅蜜" | "波羅密"'}

%h2 NOT
%p 例：#{link_to_url '/search/extended?q="迦葉" !"迦葉佛"'}

%h2 NEAR 搜尋
%p 
  例：
  =link_to_url '/search/all_in_one?q="法鼓" NEAR/7 "迦葉"'
%p NEAR/ 後面的數字 表示兩個關鍵詞中間的距離

%h3 多個詞的 NEAR
%p=link_to_url '/search/extended?q="老子" NEAR/7 "道" NEAR/3 "經"'

%p
  NEAR 查詢只回傳 num_found (符合的卷數)，不含 total_term_hits。
  需要出現次數請改用
  =link_to 'all_in_one', controller: 'static_pages', action: 'search_all_in_one'
  ，該 endpoint 會逐卷計算。

%h2 Exclude
%h3 排除前搭配
%p
  搜尋「直心」，排除「正直心」：
  =link_to_url '/search/extended?q="直心" -"正直心"'
%h3 排除後搭配
%p
  搜尋「舍利」，排除「舍利弗」：
  =link_to_url '/search/extended?q="舍利" -"舍利弗"'
%p 被排除的字串必須包含原本要搜尋的詞，否則回傳錯誤。

%h2 不支援的語法
%p
  括號分組、
  %code ~n
  (proximity)、
  %code &
  不在支援範圍，會回傳 400 錯誤。

%h2 參數
%table.table
  %tr
    %td 參數名稱
    %td
    %td 說明
  =render partial: 'search-arg-paginate'
