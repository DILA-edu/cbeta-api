get:
  summary: 下載 CBETA 某季全部 MOBI
  tags: [download]
  description: 只提供最新一季
  parameters:
    - name: quarter
      in: path
      description: 發行編號
      required: true
      schema:
        type: string
      example: 2024r3
  responses:
    '200':
      description: ZIP File
      content:
        application/zip:
          schema: 
            type: string
            format: binary
