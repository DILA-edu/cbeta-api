get:
  summary: 中文簡體轉繁體
  tags: [tool]
  parameters:
    - name: q
      in: query
      required: true
      schema:
        type: string
        example: 简体转繁体
  responses:
      '200':
        description: 轉換後的繁體中文
        content:
          text/plain:
            schema:
              type: string
              example: 簡體轉繁體
